# Render/test outputs — never commit these, from any directory.
Fdump.bin
floatimage.bin
intimage.img
image.pgm

# Parity harness (cuda/test): ephemeral build+run tree and machine-local data.
# The crystal .hkl, AMAT inputs, and CPU oracle are large/local and symlinked in
# per box; testrun/ is disposable build+render scratch. Never commit these.
cuda/workbench/testrun/
cuda/workbench/crystals/
cuda/workbench/A.mat
cuda/workbench/scaled.hkl
cuda/workbench/nanoBragg_root
